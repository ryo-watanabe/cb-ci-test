name: Repository Dispatch Example

on:
  repository_dispatch:
    types: [echo_message]

jobs:
  handle_event:
    runs-on: ubuntu-latest
    steps:
    - name: Log Event Details
      run: |
        echo "Event Type: ${{ github.event.action }}"
        echo "Payload Key: ${{ github.event.client_payload.message }}"
